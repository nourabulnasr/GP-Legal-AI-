// Widget tests require SharedPreferences and full app init.
// Use integration_test for end-to-end app testing instead.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — see integration_test/ for full app tests', () {
    expect(true, isTrue);
  });
}
